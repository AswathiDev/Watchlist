import 'package:flutter/material.dart';

class   SymbolListItem extends StatelessWidget {
  const SymbolListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}